/// @file neon_fp32_4x12.cpp
/// Auto-generated packed 4x12 FP32 NEON micro-kernel.
/// Generated by: tools/gen_neon_gemm.py
///   Mr=4, Nr=12, K_unroll=4
///   Register budget: 12 acc + 3 B + 1 A = 16 regs
///   1-pass (fits in 32 regs)

#include "dnnopt/gemm/gemm_config.h"
#include "dnnopt/gemm/gemm_ukernel_registry.h"

#include <algorithm>
#include <cstring>

#ifdef __ARM_NEON
#include <arm_neon.h>
#endif

#ifdef __aarch64__

namespace dnnopt {
// ============================================================
// Pack A: 4 contiguous floats per K
// ============================================================

static void pack_a_fp32_4x12(
        int m_len, int k_len,
        const float* A, int lda,
        float* packed_A) {
    constexpr int Mr = 4;
    for (int i = 0; i < m_len; i += Mr) {
        int m_rem = std::min(Mr, m_len - i);
        if (m_rem == Mr) {
            for (int k = 0; k < k_len; ++k) {
                packed_A[0] = A[(i + 0) * lda + k];
                packed_A[1] = A[(i + 1) * lda + k];
                packed_A[2] = A[(i + 2) * lda + k];
                packed_A[3] = A[(i + 3) * lda + k];
                packed_A += 4;
            }
        } else {
            for (int k = 0; k < k_len; ++k) {
                int r = 0;
                for (; r < m_rem; ++r)
                    packed_A[r] = A[(i + r) * lda + k];
                for (; r < Mr; ++r)
                    packed_A[r] = 0.0f;
                packed_A += 4;
            }
        }
    }
}
// ============================================================
// Pack B: 12 contiguous floats per K (3 quads)
// ============================================================

static void pack_b_fp32_4x12(
        int k_len, int n_len,
        const float* B, int ldb,
        float* packed_B) {
    constexpr int Nr = 12;
    for (int j = 0; j < n_len; j += Nr) {
        int n_rem = std::min(Nr, n_len - j);
        if (n_rem == Nr) {
            for (int k = 0; k < k_len; ++k) {
                const float* src = &B[k * ldb + j];
                vst1q_f32(packed_B + 0, vld1q_f32(src + 0));
                vst1q_f32(packed_B + 4, vld1q_f32(src + 4));
                vst1q_f32(packed_B + 8, vld1q_f32(src + 8));
                packed_B += Nr;
            }
        } else {
            for (int k = 0; k < k_len; ++k) {
                const float* src = &B[k * ldb + j];
                int c = 0;
                for (; c < n_rem; ++c)
                    packed_B[c] = src[c];
                for (; c < Nr; ++c)
                    packed_B[c] = 0.0f;
                packed_B += Nr;
            }
        }
    }
}
// ============================================================
// Packed 4x12 NEON micro-kernel (4x K-unrolled)
// ============================================================

static void gemm_ukernel_fp32_packed_4x12(
        int K,
        const float* __restrict__ packed_A,
        const float* __restrict__ packed_B,
        float* __restrict__ C, int ldc,
        float alpha, float beta) {

    float32x4_t a00 = vdupq_n_f32(0);
    float32x4_t a01 = vdupq_n_f32(0);
    float32x4_t a02 = vdupq_n_f32(0);
    float32x4_t a10 = vdupq_n_f32(0);
    float32x4_t a11 = vdupq_n_f32(0);
    float32x4_t a12 = vdupq_n_f32(0);
    float32x4_t a20 = vdupq_n_f32(0);
    float32x4_t a21 = vdupq_n_f32(0);
    float32x4_t a22 = vdupq_n_f32(0);
    float32x4_t a30 = vdupq_n_f32(0);
    float32x4_t a31 = vdupq_n_f32(0);
    float32x4_t a32 = vdupq_n_f32(0);

    const float* __restrict__ pA = packed_A;
    const float* __restrict__ pB = packed_B;

    // 4x K-unrolled main loop
    int k = 0;
    for (; k + 3 < K; k += 4) {
        if (k + 16 < K) {
            __asm__ volatile("prfm pldl1keep, [%0]" : : "r"(pA + 16) : "memory");
            __asm__ volatile("prfm pldl1keep, [%0]" : : "r"(pB + 48) : "memory");
        }

        // K iteration 0
        {
            float32x4_t ar0 = vld1q_f32(pA + 0);
            pA += 4;
            float32x4_t b0 = vld1q_f32(pB + 0);
            float32x4_t b1 = vld1q_f32(pB + 4);
            float32x4_t b2 = vld1q_f32(pB + 8);
            pB += 12;
            a00 = vfmaq_laneq_f32(a00, b0, ar0, 0);
            a01 = vfmaq_laneq_f32(a01, b1, ar0, 0);
            a02 = vfmaq_laneq_f32(a02, b2, ar0, 0);
            a10 = vfmaq_laneq_f32(a10, b0, ar0, 1);
            a11 = vfmaq_laneq_f32(a11, b1, ar0, 1);
            a12 = vfmaq_laneq_f32(a12, b2, ar0, 1);
            a20 = vfmaq_laneq_f32(a20, b0, ar0, 2);
            a21 = vfmaq_laneq_f32(a21, b1, ar0, 2);
            a22 = vfmaq_laneq_f32(a22, b2, ar0, 2);
            a30 = vfmaq_laneq_f32(a30, b0, ar0, 3);
            a31 = vfmaq_laneq_f32(a31, b1, ar0, 3);
            a32 = vfmaq_laneq_f32(a32, b2, ar0, 3);
        }

        // K iteration 1
        {
            float32x4_t ar0 = vld1q_f32(pA + 0);
            pA += 4;
            float32x4_t b0 = vld1q_f32(pB + 0);
            float32x4_t b1 = vld1q_f32(pB + 4);
            float32x4_t b2 = vld1q_f32(pB + 8);
            pB += 12;
            a00 = vfmaq_laneq_f32(a00, b0, ar0, 0);
            a01 = vfmaq_laneq_f32(a01, b1, ar0, 0);
            a02 = vfmaq_laneq_f32(a02, b2, ar0, 0);
            a10 = vfmaq_laneq_f32(a10, b0, ar0, 1);
            a11 = vfmaq_laneq_f32(a11, b1, ar0, 1);
            a12 = vfmaq_laneq_f32(a12, b2, ar0, 1);
            a20 = vfmaq_laneq_f32(a20, b0, ar0, 2);
            a21 = vfmaq_laneq_f32(a21, b1, ar0, 2);
            a22 = vfmaq_laneq_f32(a22, b2, ar0, 2);
            a30 = vfmaq_laneq_f32(a30, b0, ar0, 3);
            a31 = vfmaq_laneq_f32(a31, b1, ar0, 3);
            a32 = vfmaq_laneq_f32(a32, b2, ar0, 3);
        }

        // K iteration 2
        {
            float32x4_t ar0 = vld1q_f32(pA + 0);
            pA += 4;
            float32x4_t b0 = vld1q_f32(pB + 0);
            float32x4_t b1 = vld1q_f32(pB + 4);
            float32x4_t b2 = vld1q_f32(pB + 8);
            pB += 12;
            a00 = vfmaq_laneq_f32(a00, b0, ar0, 0);
            a01 = vfmaq_laneq_f32(a01, b1, ar0, 0);
            a02 = vfmaq_laneq_f32(a02, b2, ar0, 0);
            a10 = vfmaq_laneq_f32(a10, b0, ar0, 1);
            a11 = vfmaq_laneq_f32(a11, b1, ar0, 1);
            a12 = vfmaq_laneq_f32(a12, b2, ar0, 1);
            a20 = vfmaq_laneq_f32(a20, b0, ar0, 2);
            a21 = vfmaq_laneq_f32(a21, b1, ar0, 2);
            a22 = vfmaq_laneq_f32(a22, b2, ar0, 2);
            a30 = vfmaq_laneq_f32(a30, b0, ar0, 3);
            a31 = vfmaq_laneq_f32(a31, b1, ar0, 3);
            a32 = vfmaq_laneq_f32(a32, b2, ar0, 3);
        }

        // K iteration 3
        {
            float32x4_t ar0 = vld1q_f32(pA + 0);
            pA += 4;
            float32x4_t b0 = vld1q_f32(pB + 0);
            float32x4_t b1 = vld1q_f32(pB + 4);
            float32x4_t b2 = vld1q_f32(pB + 8);
            pB += 12;
            a00 = vfmaq_laneq_f32(a00, b0, ar0, 0);
            a01 = vfmaq_laneq_f32(a01, b1, ar0, 0);
            a02 = vfmaq_laneq_f32(a02, b2, ar0, 0);
            a10 = vfmaq_laneq_f32(a10, b0, ar0, 1);
            a11 = vfmaq_laneq_f32(a11, b1, ar0, 1);
            a12 = vfmaq_laneq_f32(a12, b2, ar0, 1);
            a20 = vfmaq_laneq_f32(a20, b0, ar0, 2);
            a21 = vfmaq_laneq_f32(a21, b1, ar0, 2);
            a22 = vfmaq_laneq_f32(a22, b2, ar0, 2);
            a30 = vfmaq_laneq_f32(a30, b0, ar0, 3);
            a31 = vfmaq_laneq_f32(a31, b1, ar0, 3);
            a32 = vfmaq_laneq_f32(a32, b2, ar0, 3);
        }
    }

    // K tail
    for (; k < K; ++k) {
        float32x4_t ar0 = vld1q_f32(pA + 0);
        pA += 4;
        float32x4_t b0 = vld1q_f32(pB + 0);
        float32x4_t b1 = vld1q_f32(pB + 4);
        float32x4_t b2 = vld1q_f32(pB + 8);
        pB += 12;
        a00 = vfmaq_laneq_f32(a00, b0, ar0, 0);
        a01 = vfmaq_laneq_f32(a01, b1, ar0, 0);
        a02 = vfmaq_laneq_f32(a02, b2, ar0, 0);
        a10 = vfmaq_laneq_f32(a10, b0, ar0, 1);
        a11 = vfmaq_laneq_f32(a11, b1, ar0, 1);
        a12 = vfmaq_laneq_f32(a12, b2, ar0, 1);
        a20 = vfmaq_laneq_f32(a20, b0, ar0, 2);
        a21 = vfmaq_laneq_f32(a21, b1, ar0, 2);
        a22 = vfmaq_laneq_f32(a22, b2, ar0, 2);
        a30 = vfmaq_laneq_f32(a30, b0, ar0, 3);
        a31 = vfmaq_laneq_f32(a31, b1, ar0, 3);
        a32 = vfmaq_laneq_f32(a32, b2, ar0, 3);
    }

    // Epilogue: store C = alpha * acc + beta * C
    float32x4_t av = vdupq_n_f32(alpha);
    float32x4_t bv = vdupq_n_f32(beta);
    {
        float* cr = C + 0 * ldc;
        if (beta == 0.0f) {
            vst1q_f32(cr, vmulq_f32(av, a00));
            vst1q_f32(cr + 4, vmulq_f32(av, a01));
            vst1q_f32(cr + 8, vmulq_f32(av, a02));
        } else {
            vst1q_f32(cr, vfmaq_f32(vmulq_f32(av, a00), bv, vld1q_f32(cr)));
            vst1q_f32(cr + 4, vfmaq_f32(vmulq_f32(av, a01), bv, vld1q_f32(cr + 4)));
            vst1q_f32(cr + 8, vfmaq_f32(vmulq_f32(av, a02), bv, vld1q_f32(cr + 8)));
        }
    }
    {
        float* cr = C + 1 * ldc;
        if (beta == 0.0f) {
            vst1q_f32(cr, vmulq_f32(av, a10));
            vst1q_f32(cr + 4, vmulq_f32(av, a11));
            vst1q_f32(cr + 8, vmulq_f32(av, a12));
        } else {
            vst1q_f32(cr, vfmaq_f32(vmulq_f32(av, a10), bv, vld1q_f32(cr)));
            vst1q_f32(cr + 4, vfmaq_f32(vmulq_f32(av, a11), bv, vld1q_f32(cr + 4)));
            vst1q_f32(cr + 8, vfmaq_f32(vmulq_f32(av, a12), bv, vld1q_f32(cr + 8)));
        }
    }
    {
        float* cr = C + 2 * ldc;
        if (beta == 0.0f) {
            vst1q_f32(cr, vmulq_f32(av, a20));
            vst1q_f32(cr + 4, vmulq_f32(av, a21));
            vst1q_f32(cr + 8, vmulq_f32(av, a22));
        } else {
            vst1q_f32(cr, vfmaq_f32(vmulq_f32(av, a20), bv, vld1q_f32(cr)));
            vst1q_f32(cr + 4, vfmaq_f32(vmulq_f32(av, a21), bv, vld1q_f32(cr + 4)));
            vst1q_f32(cr + 8, vfmaq_f32(vmulq_f32(av, a22), bv, vld1q_f32(cr + 8)));
        }
    }
    {
        float* cr = C + 3 * ldc;
        if (beta == 0.0f) {
            vst1q_f32(cr, vmulq_f32(av, a30));
            vst1q_f32(cr + 4, vmulq_f32(av, a31));
            vst1q_f32(cr + 8, vmulq_f32(av, a32));
        } else {
            vst1q_f32(cr, vfmaq_f32(vmulq_f32(av, a30), bv, vld1q_f32(cr)));
            vst1q_f32(cr + 4, vfmaq_f32(vmulq_f32(av, a31), bv, vld1q_f32(cr + 4)));
            vst1q_f32(cr + 8, vfmaq_f32(vmulq_f32(av, a32), bv, vld1q_f32(cr + 8)));
        }
    }
}
// ============================================================
// Registry wrappers + auto-registration
// ============================================================

namespace {

void ukernel_fp32_4x12_wrap(
        int K, const void* packed_A, const void* packed_B,
        float* C, int ldc, float alpha, float beta,
        float /*extra*/) {
    gemm_ukernel_fp32_packed_4x12(
        K,
        static_cast<const float*>(packed_A),
        static_cast<const float*>(packed_B),
        C, ldc, alpha, beta);
}

void pack_a_fp32_4x12_wrap(
        int m_len, int k_len, const float* A, int lda,
        void* packed_A, int /*Mr*/, float* /*scale_out*/) {
    pack_a_fp32_4x12(m_len, k_len, A, lda, static_cast<float*>(packed_A));
}

void pack_b_fp32_4x12_wrap(
        int k_len, int n_len, const float* B, int ldb,
        void* packed_B, int /*Nr*/, float* /*scale_out*/) {
    pack_b_fp32_4x12(k_len, n_len, B, ldb, static_cast<float*>(packed_B));
}

// Priority 105
const GemmMicrokernelDesc neon_fp32_4x12_desc = {
    "neon_fp32_4x12",
    GemmDataType::kFP32,
    kNEON,
    4,                    // Mr
    12,                   // Nr
    1,                    // Kgroup
    false,                // nr_is_vla
    105,                  // priority
    sizeof(float),        // packed_a_elem_bytes
    sizeof(float),        // packed_b_elem_bytes
    0,                    // min_sve_bits
    ukernel_fp32_4x12_wrap,
    pack_a_fp32_4x12_wrap,
    pack_b_fp32_4x12_wrap,
};

static RegisterKernel reg_neon_fp32_4x12(neon_fp32_4x12_desc);

}  // namespace

}  // namespace dnnopt

#endif  // __aarch64__
